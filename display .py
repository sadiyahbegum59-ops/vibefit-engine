Recommended Outfit:
Black coord set + heels

Aesthetic Compatibility:
92%
def calculate_score(user_vibe, outfit):

    score = 0

    if outfit["mood"] == user_vibe:
        score += 40

    if outfit["occasion"] == occasion:
        score += 30

    if outfit["style"] == preferred_style:
        score += 30

    return score
